# 06.26 시험 D-1 코테에요

문제 읽기 → 뭘 구해야 하지?
패턴/구조 찾기 → 반복? 순환? 최댓값 추적?
자료구조 선택 → 리스트? 딕셔너리? 큐?
코드 작성


### 2차원 배열 순회하는 방법
for i in sizes:      # i = [60, 50]
for w, h in sizes:   # w = 60, h = 50

### enumerate : 인덱스랑 배열의 값을 같이 꺼내는 방법.
for i, item in enumerate(items):
    print(f"{i}번 상품: {item}")

0번째 상품 (item) << 이런식으로 출력됨.

## from collections import Counter

dict으로 편하게 변환 후에 문제를 풀 수 있다.
    EX) dict1 = Counter(배열)

    result2 = list(dict1.keys())
    result3 = list(dict1.values())
    이 부분도 중요하다. 키값만 따로 배열로 옮길 수 있다!!

### 해시 - 폰켓몬

set = 중복 제거
a = set(b)
    - b의 중복을 제거하고 a에 박는다.


# 오늘치 복습 꼭 하기! 

최소 직사각형
모의고사
카펫



from collections import Counter 
def eee(words):
    w1 = Counter(words)
    w2 = max(w1.values())
    result1 = list(w2.keys())
    rer = result.sort()
    return rer[0]
   