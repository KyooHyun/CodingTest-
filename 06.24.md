# 하나은행 체험형 인턴 코테 대비 SQL고득점 KIT

내가 알아아할 개념 정리

### 반올림
ROUND(숫자, 2)
- 2에서 반올림하세요
FLOOR(숫자)
- 무조건 내림
TRUNCATE(숫자,2)
소숫점 2자리에서부터는 다잘라.

### LEFT JOIN
필요한걸 옆으로 붙이는 느낌으로
FROM A 
LEFT JOIN A ON B

SELECT CUSTOMER_ID, SUM(AMOUNT) AS TOTAL_AMOUNT

FROM ORDERS 

WHERE MONTH(ORDER_DATE) = '3'

GROUP BY CUSTOMER_ID

ORDER BY TOTAL_AMOUNT DESC

